# Frontend — SAFE to expose (no API secret here)
# Upload preset must be set to UNSIGNED in Cloudinary dashboard:
# https://cloudinary.com → Settings → Upload → Upload presets → Add preset → Signing Mode: Unsigned
VITE_CLOUDINARY_CLOUD_NAME=dnqvijzxj
VITE_CLOUDINARY_UPLOAD_PRESET=apna_bazarr_uploads
# VITE_API_URL=https://apnabazarr-backend.onrender.com
VITE_API_URL=http://localhost:5000
